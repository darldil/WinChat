                   		WINCHAT FOR WINDOWS

WinChat is an experimental chat client based on one of the practices of the Universidad Complutense of Madrid. 
The application is single user and works offline. 
To function should create a folder called "WCTMP", inside this folder you must create a file called ”usuarios.dat", which contains the following structure:

- Username
- Password
- Date (put 0 at first time)
- 0 (Number of Messages unread)

At the end of the document add xxx to indicate the end of this